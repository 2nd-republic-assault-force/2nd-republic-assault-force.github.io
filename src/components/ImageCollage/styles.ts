import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
      imageContainer: {
            width: "500px",
            height: "450px",
      },
}));
